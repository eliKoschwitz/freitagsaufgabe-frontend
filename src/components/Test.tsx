

function Test(props:{status:string, stateToPass:Function}){

    return(
        <div>
            hier wird der status gegeben {props.status}
        <select>

        </select>

        </div>

    )
}

export default Test;